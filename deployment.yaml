apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-limiter
  labels:
    app: resource-limiter
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
        maxSurge: 2
        maxUnavailable: 0
  selector:
    matchLabels:
      app: resource-limiter
  template:
    metadata:
      labels:
        app: resource-limiter
    spec:
      containers:
      - name: resource-limiter
        image: eiachh/resource-limiter
        imagePullPolicy: "Never"
        ports:
        - containerPort: 5000